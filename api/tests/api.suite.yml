actor: ApiTester
modules:
    enabled:
        - Asserts
        - REST:
            url: http://localhost
            depends: PhpBrowser
            part: Json
        - Db:
            dsn: "mysql:host=%MYSQL_HOST%;dbname=%MYSQL_DATABASE%"
            user: "%MYSQL_USER%"
            password: "%MYSQL_PASSWORD%"
