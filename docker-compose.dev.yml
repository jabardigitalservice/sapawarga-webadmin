version: '3'
services:
  app:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: registry.gitlab.com/jdsteam/sapa-warga/sapawarga-webadmin:${IMAGETAG}
    networks:
      - internal
    ports: 
      - 8080:80

networks:
  internal:
    driver: bridge
